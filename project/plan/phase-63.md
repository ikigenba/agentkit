# Phase 63 — live integration: uniform happy-path suite, OpenRouter reported cost & ChatGPT-subscription round trip

*Realizes design Decisions 24 and 25 (live ids) and the uniform live happy-path slice of Decision 13 (R-CL9K-41F1, R-CMHG-HT5Q, R-CNPC-VKWF, R-COX9-9CN4, R-CQ55-N4DT, R-CRD2-0W4I, R-CSKY-ENV7, R-CTSU-SFLW). Depends on Phases 58 and 59.*

Every chat provider package carries the same two presence-gated live tests — a plain round trip and a tool-call round trip — each skipping cleanly when its credential env var is absent, per D13's uniform live happy-path suite. Cells already proven by stronger provider-specific live ids are not duplicated: google's tool-call cell is R-9UK4-JI3L (existing), openrouter's plain cell is R-DF22-UT85 (this phase). On top of the uniform suite, the two claims that hinge on real external contracts are proven against the real services: a real OpenRouter round trip returning a provider-reported cost, and a real ChatGPT-backend round trip authenticated from a locally present codex-compatible auth file (read/use only — no refresh forced against the shared-lineage file).

**Done when:** the suite is green; with the matching credential present each uniform live test completes its real round trip — R-CL9K-41F1/R-CMHG-HT5Q (anthropic, `ANTHROPIC_API_KEY`), R-CNPC-VKWF (google, `GEMINI_API_KEY`/`GOOGLE_API_KEY`), R-COX9-9CN4/R-CQ55-N4DT (openai, `OPENAI_API_KEY`), R-CRD2-0W4I/R-CSKY-ENV7 (zai, `ZAI_API_KEY`), R-CTSU-SFLW (openrouter tool call, `OPENROUTER_API_KEY`); with `OPENROUTER_API_KEY` present, the R-DF22-UT85 test completes a real round trip with `ReportedCost()` present and > 0; with the local auth file present, the R-DJXO-DW6X test completes a real subscription round trip with a non-error assembled message; every live test skips (not fails) when its gate is absent.
