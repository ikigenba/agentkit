# Phase 14 — Example REPL · extracted

An example REPL was built in this phase as a thin consumer of the public API. It has since been **extracted into its own standalone project (`agentrepl`)** and is no longer part of this repository, so its design decision (former Decision 14) and `examples/repl/` tree have been removed. This entry is retained only as construction history; nothing in this repo realizes it anymore.

